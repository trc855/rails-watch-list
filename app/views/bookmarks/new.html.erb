<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>

<%# TODO: fix list association so it is automatic %>
